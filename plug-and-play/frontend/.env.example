# For local dev — Vite proxy handles /api → localhost:8000, so leave blank
VITE_API_URL=

# For production on Vercel — set to your Render backend URL
# VITE_API_URL=https://plug-and-play-api.onrender.com/api
